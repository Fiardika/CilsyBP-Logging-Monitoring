apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: kubernetes-dashboard
  name: ingress
spec:
  rules:
  - host: dash.fiardika.xyz
    http:
      paths:
        - backend:
           serviceName: kubernetes-dashboard
           servicePort: 80
